{
  "name": "struktolab-root",
  "author": "OpenPatch",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.10",
    "typescript": "^5.7.2"
  },
  "engines": {
    "pnpm": ">=9"
  }
}
